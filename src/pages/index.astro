<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>我的个人主页 - HJ</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', system-ui, sans-serif;
      color: #fff;
      background: #000;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* 背景图片 */
    .bg {
      position: fixed;
      inset: 0;
      background: url('https://i1.hdslb.com/bfs/new_dyn/231d01e18b55ad128caa42fbded000a716193877.jpg@1192w.avif') center/cover no-repeat;
      z-index: -2;
    }

    .bg::after {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.65); /* 深色遮罩，让文字更清晰 */
      z-index: -1;
    }

    /* 主要内容居中 */
    .container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 2rem;
      position: relative;
    }

    .avatar {
      width: 180px;
      height: 180px;
      border-radius: 50%;
      object-fit: cover;
      border: 5px solid rgba(255,255,255,0.3);
      margin-bottom: 1.5rem;
      box-shadow: 0 10px 30px rgba(0,0,0,0.6);
    }

    h1 {
      font-size: 3.5rem;
      margin-bottom: 0.8rem;
      text-shadow: 0 4px 10px rgba(0,0,0,0.7);
    }

    .subtitle {
      font-size: 1.4rem;
      opacity: 0.9;
      margin-bottom: 2rem;
      max-width: 600px;
    }

    .bio {
      font-size: 1.15rem;
      line-height: 1.7;
      max-width: 700px;
      margin-bottom: 2.5rem;
      background: rgba(0,0,0,0.4);
      padding: 1.5rem;
      border-radius: 12px;
      backdrop-filter: blur(6px);
    }

    .social-links {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 2.5rem;
    }

    .social-links a {
      color: white;
      font-size: 1.8rem;
      transition: all 0.3s;
      opacity: 0.85;
    }

    .social-links a:hover {
      transform: translateY(-6px);
      opacity: 1;
      color: #00d4ff;
    }

    /* 音乐控制按钮 */
    .music-control {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: rgba(0,0,0,0.5);
      color: white;
      border: none;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      font-size: 1.6rem;
      cursor: pointer;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0,0,0,0.5);
      transition: all 0.3s;
      z-index: 100;
    }

    .music-control:hover {
      background: rgba(0,212,255,0.3);
      transform: scale(1.1);
    }

    /* 响应式 */
    @media (max-width: 768px) {
      h1 { font-size: 2.8rem; }
      .subtitle { font-size: 1.2rem; }
      .avatar { width: 140px; height: 140px; }
    }
  </style>
</head>
<body>
  <!-- 背景图层 -->
  <div class="bg"></div>

  <div class="container">
    <img src="https://i1.hdslb.com/bfs/new_dyn/1db74c691ec5bc376c5c75fbb218bd3d16193877.png@1192w.avif" alt="我的头像" class="avatar">

    <h1>ran yu</h1>
    <div class="subtitle">前端开发者 | 音乐爱好者 | 生活记录者</div>

    <div class="bio">
      <p>大家好，我是 ranyu，目前居住在洛杉矶。</p>
      <p>喜欢用代码创造有趣的东西，也喜欢听歌、旅行、摄影。</p>
      <p>这里是我的小角落，记录一些想法、项目和日常。</p>
      <p>欢迎和我交流～</p>
    </div>

    <div class="social-links">
      <a href="https://github.com/liupuzhi" target="_blank"><i class="fab fa-github"></i></a>
      <a href="https://twitter.com/" target="_blank"><i class="fab fa-twitter"></i></a>
      <a href="https://www.instagram.com/" target="_blank"><i class="fab fa-instagram"></i></a>
      <a href="mailto:your@email.com"><i class="fas fa-envelope"></i></a>
    </div>

    <!-- 背景音乐 -->
    <audio id="bgMusic" loop>
      <!-- 替换成你自己的音乐链接，支持 mp3 / ogg 等 -->
      <source src="https://github.com/liupuzhi/mysite/blob/main/assets/surprising_media-romantic-acoustic-guitar-1-433555.mp3" type="audio/mpeg">
      您的浏览器不支持 audio 元素。
    </audio>

    <button class="music-control" id="musicBtn">
      <i class="fas fa-volume-mute"></i>
    </button>
  </div>

  <script>
    const music = document.getElementById('bgMusic');
    const btn = document.getElementById('musicBtn');
    let isPlaying = false;

    // 浏览器策略：自动播放通常需要用户交互后才能生效
    btn.addEventListener('click', () => {
      if (isPlaying) {
        music.pause();
        btn.innerHTML = '<i class="fas fa-volume-mute"></i>';
      } else {
        music.play().catch(e => console.log("自动播放被阻止，需要用户交互"));
        btn.innerHTML = '<i class="fas fa-volume-up"></i>';
      }
      isPlaying = !isPlaying;
    });

    // 可选：页面加载后尝试播放（很多浏览器会阻塞）
    // music.volume = 0.2;
    // music.play().catch(() => {});
  </script>
</body>
</html>